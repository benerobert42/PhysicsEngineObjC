#include "Engine.h"
#include "RigidBody.h"
#import <Cocoa/Cocoa.h>

int main(int argc, const char * argv[]) {
//    PhysicsEngine engine;
//    RigidBody body_1;
//    RigidBody body_2{._position = {0, 0, 0.5}, ._velocity = {0.5, 0.5, 0}};
//    engine.addBody(body_1);
//    engine.addBody(body_2);
//    while (engine._lastDrawTime < 25) {
//        engine.update();
//    for (RigidBody& body : engine._bodies) {
//        bindVertexBytes(renderEncoder, body._modelTransform.data(), 2)
//    }
//    }
    return NSApplicationMain(argc, argv);
}
